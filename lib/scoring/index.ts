/** Sistema de puntuación de intención — implementación en PASO 4 */

import type { IntentScoreResult } from "@/types";

export async function scorePost(): Promise<IntentScoreResult> {
  throw new Error("Scoring no implementado — PASO 4");
}
